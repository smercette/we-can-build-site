{
  "name": "we-can-build-site",
  "version": "1.0.0",
  "description": "We Can Build — Sarah J Mercer's site",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
